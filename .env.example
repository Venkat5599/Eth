# ---- Frontend (Vercel) ----
# The deployed Grants DAO contract on Arbitrum Sepolia.
NEXT_PUBLIC_DAO_ADDRESS=0xd082835164c1f83110a7efa1097edac998988f72
# Public RPC the frontend reads from (writes go through the user's wallet).
NEXT_PUBLIC_ARB_RPC=https://sepolia-rollup.arbitrum.io/rpc

# ---- Contract deploy (only needed to deploy/admin the contract) ----
# Funded Arbitrum Sepolia deployer key. NEVER commit a real one.
PRIVATE_KEY=0x...
ARB_SEPOLIA_RPC=https://sepolia-rollup.arbitrum.io/rpc
